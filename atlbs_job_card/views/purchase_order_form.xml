<odoo>
    <record id="view_purchase_order_form_inherit_vehicle" model="ir.ui.view">
        <field name="name">purchase.order.form.vehicle.inherit</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">

            <!-- Add button before order_line -->
            <xpath expr="//field[@name='order_line']" position="before">
                <button name="action_open_vehicle_products"
                        string="Add Vehicle Products"
                        type="object"
                        class="btn-primary"/>
            </xpath>


            <!-- Add department field in purchase.order.line -->
            <xpath expr="//field[@name='order_line']/list/field[@name='product_id']" position="before">
                <field name="department"/>
            </xpath>
             <xpath expr="//field[@name='order_line']/list/field[@name='product_id']" position="after">
                <field name="description"/>
            </xpath>


            <!-- Add stock_qty after qty -->
            <xpath expr="//field[@name='order_line']/list/field[@name='product_qty']" position="after">
                <field name="stock_qty"/>
                <field name="product_location_id"/>
            </xpath>


        </field>
    </record>


    <record id="action_purchase_vehicle_products" model="ir.actions.act_window">
        <field name="name">Vehicles</field>
        <field name="res_model">product.template</field>
        <field name="view_mode">list,kanban,form</field>
        <field name="domain">[('categ_id.name','=','Vehicles')]</field>
<!--        here i have given id instead of string if is there any change in future we can change this id-->
        <field name="context">{'default_categ_id': 4, 'default_is_storable': True}</field>
    </record>

    <menuitem id="menu_vehicle_products" name="Vehicles" parent="purchase.menu_purchase_root" sequence="100"/>
    <menuitem id="submenu_all_vehicles" name="All Vehicles" parent="menu_vehicle_products"
              action="action_purchase_vehicle_products" sequence="1"/>


</odoo>
